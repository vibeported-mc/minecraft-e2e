modLoader = "javafml"
loaderVersion = "[0,)"
license = "MIT"

# Declared as a mod purely so FancyModLoader puts it in the same module layer as the e2e mod that
# depends on it. It has no entrypoint and does nothing on its own; the orchestrator, which is a plain
# JVM with no mod loader anywhere near it, ignores this file entirely.
[[mods]]
modId = "e2e_protocol"
version = "0.1.0"
displayName = "Minecraft E2E protocol"
description = '''Wire types shared by the e2e framework and its orchestrator.'''
